[project]
name = "myna"
version = "0.0.0"
description = "Linux 桌面语音输入法：快捷键唤醒，文字直接输入到当前焦点窗口"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = []

[project.scripts]
myna = "myna.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

# 图标和 .desktop 模板要跟着包走，否则 pip 装完 install 找不到它们
[tool.setuptools.package-data]
myna = ["assets/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
